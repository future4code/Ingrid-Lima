import { type } from "os"

export type user = {
   id: string
   name: string
   nickname: string
   email: string
   address: string
   zipcode: string,
   number: string,
   complement: string
}

export type adressInfo ={
   street: string,
   neighborhood: string,
   city: string,
   state: string,
   
}